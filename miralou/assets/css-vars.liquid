<style>
  /* Header Container */
  .site-header .header-container {
    max-width: 75rem;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 2rem;

  }

  /* Left - Logo */
  .site-header .logo {
    font-family: 'Barlow', serif;
    font-size: 2rem;
    font-weight: bold;
  }

  /* Center - Navigation */
  .site-header .nav-links {
    display: flex;
    gap: 2rem;
    align-items: center;
  }

  .site-header .nav-links a {
    text-decoration: none;
    color: black;
    font-weight: 600;
    font-size: 0.95rem;
    position: relative;
  }

  .site-header .nav-links a.active {
    color: #e63946;
    /* Red color for active link */
  }

  .uppercase {
    text-transform: uppercase;
  }

  /* Right - Icons */
  .site-header .icons {
    display: flex;
    gap: 1.5rem;
    align-items: center;
  }

  .site-header .icons svg {
    width: 20px;
    height: 20px;
    stroke: black;
    fill: none;
    stroke-width: 2;
    cursor: pointer;
  }

  .dropdown-icon-style {
    width: 10px;
    height: 10px;
    stroke: black;
    fill: none;
    stroke-width: 2;
    cursor: pointer;
    padding-bottom: 10px;
  }

  .no-wrap-text {
    white-space: nowrap;
    font-size: 1rem;
    /* text-base = 16px = 1rem */
    color: #6b7280;
    /* text-gray-500 */
    transition: color 0.2s ease-in-out;
  }

  .no-wrap-text:hover {
    color: #111827;
    /* text-gray-900 */
  }


  .nav-links {
    display: flex;
    gap: 2rem;
    align-items: center;
    position: relative;
  }

  .nav-item {
    position: relative;
  }

  .nav-link {
    font-size: 1rem;
    font-weight: 600;
    color: #000;
    text-decoration: none;
    white-space: nowrap;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
  }

  .nav-link.with-dropdown:hover {
    color: #e63946;
    /* Highlight on hover */
  }

  .dropdown {
    position: absolute;
    top: 100%;
    left: 0;
    background: white;
    border: 1px solid #eee;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
    min-width: 150px;
    z-index: 20;
    padding: 0.5rem 0;
    display: flex;
    flex-direction: column;
  }

  .dropdown-link {
    padding: 0.5rem 1rem;
    text-decoration: none;
    color: #333;
    font-weight: 500;
    white-space: nowrap;
  }

  .dropdown-link:hover {
    background-color: #f3f3f3;
    color: #000;
  }


  .site-footer {
    background-color: #1f1f1f;
    color: #fff;
    padding: 3rem 2rem;
    font-family: 'Poppins', sans-serif;
  }

  .footer-container {
    max-width: 75rem;
    margin: 0 auto;
  }

  .footer-newsseller .newsseller-container {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding-bottom: 3rem;
    flex-wrap: wrap;
    gap: 2rem;
    margin: 50px;
  }

  .footer-newsseller .newsseller-container h2 {
    font-size: 1.8rem;
    font-weight: 700;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }

  .footer-newsseller .newsseller-container p {
    max-width: 400px;
    color: #ccc;
  }

  .footer-newsseller .newsseller-container .discount {
    color: #e63946;
    font-weight: 600;
  }

  .newsseller-form {
    display: flex;
    flex: 1;
    max-width: 600px;
    justify-content: flex-end;
  }

  .newsseller-form input {
    padding: 0.75rem 1rem;
    border: 1px solid #e63946;
    background: transparent;
    color: #fff;
    font-size: 1rem;
    flex: 1;
    margin-right: 1rem;
  }

  .newsseller-form button {
    background: #e63946;
    border: none;
    padding: 0.75rem 1.5rem;
    color: #fff;
    font-weight: 600;
    cursor: pointer;
    border-radius: 0.3rem;
    min-width: 150px;
  }

  .footer-columns {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    padding: 3rem 0;
    border-top: 1px solid #e63946;
  }

  .footer-brand h3 {
    font-family: 'Barlow', serif;
    font-size: 1.8rem;
    font-weight: 700;
  }

  .footer-brand p {
    color: #aaa;
    margin: 1rem 0;
  }

  .social-icons a {
    margin-right: 1rem;
    font-size: 1.25rem;
    color: #fff;
    text-decoration: none;
  }

  .footer-menu h4,
  .footer-contact h4 {
    color: #e63946;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }

  .footer-menu ul,
  .footer-contact ul {
    list-style: none;
    padding: 0;
  }

  .footer-menu ul li,
  .footer-contact ul li {
    margin-bottom: 0.5rem;
    color: #ccc;
  }

  .footer-menu ul li a {
    text-decoration: none;
    color: #ccc;
  }

  .footer-menu ul li a:hover {
    color: #fff;
  }

  .footer-bottom {
    text-align: right;
    font-weight: 700;
    text-transform: uppercase;
    border-top: none;
    padding-top: 1rem;
    margin-top: 1rem;
  }

  .footer-bottom-line {
    border-top: 1px solid #e63946;
    margin-bottom: 0.5rem;
  }

  .footer-bottom-copy {
    margin-top: 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    text-align: left;
  }

  .footer-bottom-copy p {
    margin: 0;
  }

  .rounded-1 {
    border-radius: 5px;
  }inter;
    }
</style>
