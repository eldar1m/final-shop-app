<section class="newsletter">
  <div class="newsletter__inner page-width">
    <div class="newsletter__text">
      <h2 class="newsletter__title">{{ section.settings.heading }}</h2>
      <div class="newsletter__subtext">{{ section.settings.subtext }}</div>
    </div>
    <form
      method="post"
      action="/contact#newsletter-footer"
      accept-charset="UTF-8"
      class="newsletter__form"
      id="newsletter-footer"
    >
      <input type="hidden" name="form_type" value="customer">
      <input type="hidden" name="utf8" value="✓">
      <input type="email" name="contact[email]" placeholder="{{ section.settings.placeholder }}" required>
      <button type="submit">{{ section.settings.button_label }}</button>
    </form>
  </div>
</section>

{% schema %}
{
  "name": "Newsletter",
  "limit": 1,
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "OUR NEWSSELLER"
    },
    {
      "type": "richtext",
      "id": "subtext",
      "label": "Subtext",
      "default": "<p>Subscribe to get more useful information about us and to get you <strong>15% discount</strong> off your next purchase</p>"
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Email placeholder",
      "default": "Enter your e-mail"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Subscribe"
    }
  ],
  "presets": [
    {
      "name": "Newsletter"
    }
  ]
}
{% endschema %}
